import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export const Events = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Events
      </Typography>
      <Typography color="text.secondary">
        Guild events will appear here.
      </Typography>
    </Box>
  );
};